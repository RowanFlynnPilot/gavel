# Gavel engine dependencies (Python 3.12+)
anthropic>=0.40
requests>=2.31
yt-dlp>=2025.1.1
youtube-transcript-api>=1.0
pdfplumber>=0.11
# Digest rendering (engine/digest.py)
Pillow>=10.0
# Optional, local/residential only — audio transcription fallback:
#   python -m pip install faster-whisper
